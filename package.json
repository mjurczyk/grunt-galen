{
  "name": "grunt-galen",
  "version": "0.4.4",
  "description": "Grunt plugin for Galen",
  "files": [
    "tasks",
    "lib"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/mjurczyk/grunt-galen"
  },
  "keywords": [
    "grunt",
    "galen"
  ],
  "engines": {
    "node": ">=0.10.0"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-cli": "^0.1.13"
  },
  "author": "mjurczyk",
  "license": "MIT",
  "dependencies": {
    "async": "^0.9.0"
  },
  "scripts": {
    "test": "npm install && cd test && npm install && grunt" 
  }
}
